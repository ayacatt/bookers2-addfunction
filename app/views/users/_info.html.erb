<table class='table'>
  <tr><%= attachment_image_tag(user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
	</tr>
	<tr>
	  <th>follows</th>
	  <th><%= link_to user.relationships.count, user_followings_path(user) %></th>
	</tr>
	<tr>
	  <th>followers</th>
	  <th><%= link_to user.relationships.count, user_followers_path(user) %></th>
	</tr>

</table>

<div class='row'>

  <%= link_to '',edit_user_path(user),class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user_#{current_user.id}" %>
　<% else %>
　<%= link_to '',edit_user_path(user),class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user_#{current_user.id}" %>
　<% end %>
</div>